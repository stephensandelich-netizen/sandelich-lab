/**
 * Compatibility entry for existing imports and template updates.
 * Personalize the root site.config.ts file instead.
 */
export { siteConfig } from "../site.config";
export { default } from "../site.config";
